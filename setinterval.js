function greet(name,chainel){ 
    console.log("This is the SetTimeout"+ name + ": " + chainel);
    
    }
    setTimeout(greet,5000,"Ankita","Back to source")
 
    timeout = setTimeout(greet,5000,"sanket is bad boy")
    console,log(timeout);
 
  //This is the DISPLAY TIME FUNCTION 
    /*function displayTime(){
     time = new Date();
     console.log(time);
  document.getElementById("time").innerHTML = time;
 }
 setInterval(displayTime, 1000);
 */